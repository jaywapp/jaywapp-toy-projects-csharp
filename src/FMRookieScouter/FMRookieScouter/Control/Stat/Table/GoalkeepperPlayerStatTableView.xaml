<UserControl x:Class="FMRookieScouter.Control.Stat.Table.GoalkeepperPlayerStatTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FMRookieScouter.Control.Stat.Table"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <local:StatTableView Grid.Column="0" DataContext="{Binding Goalkeeping}"/>
        <local:StatTableView Grid.Column="1" DataContext="{Binding Mental}"/>
        <local:StatTableView Grid.Column="2" DataContext="{Binding Technical}"/>
        <local:StatTableView Grid.Column="3" DataContext="{Binding Physical}"/>
    </Grid>
</UserControl>
