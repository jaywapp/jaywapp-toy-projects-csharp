<UserControl x:Class="FMRookieScouter.Control.ListBox.PossibilityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="SpecHeader">
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="SpecValue">
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Style="{StaticResource SpecHeader}" Text="Ability"/>
        <TextBlock Grid.Row="1" Style="{StaticResource SpecValue}" Text="{Binding Ability}"/>
        <TextBlock Grid.Row="2" Style="{StaticResource SpecHeader}" Text="Potential"/>
        <TextBlock Grid.Row="3" Style="{StaticResource SpecValue}" Text="{Binding Potential}"/>
    </Grid>
</UserControl>
