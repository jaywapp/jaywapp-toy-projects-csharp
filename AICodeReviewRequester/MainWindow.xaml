<Window x:Class="AICodeReviewRequester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AICodeReviewRequester"
        xmlns:busyIndicator="https://github.com/Peoky/BusyIndicator"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>


        <ComboBox Grid.Row="0" Grid.Column="0"
                  ItemsSource="{Binding Models, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding SelectedModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" TextAlignment="Center"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
            
        </ComboBox>

        <TextBox Grid.Row="0" Grid.Column="1" 
                 Text="{Binding Prompt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Row="0" Grid.Column="2" Content="Post" 
                Command="{Binding PostCommand}"/>

        <busyIndicator:BusyMask x:Name="BusyIndicator" 
                                Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" 
                                IndicatorType="Dashes" 
                                IsBusy="{Binding IsBusy, UpdateSourceTrigger=PropertyChanged}" 
                                BusyContent="{Binding BusyText, UpdateSourceTrigger=PropertyChanged}" >
            <TextBox IsReadOnly="True" Text="{Binding PostResult, UpdateSourceTrigger=PropertyChanged}"/>
        </busyIndicator:BusyMask>
    </Grid>
</Window>
